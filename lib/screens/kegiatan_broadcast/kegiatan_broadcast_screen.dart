import 'package:flutter/material.dart';

class KegiatanBroadcastScreen extends StatelessWidget {
  const KegiatanBroadcastScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Kegiatan & Broadcast Screen'));
  }
}
