import 'package:flutter/material.dart';

class LogAktifitasScreen extends StatelessWidget {
  const LogAktifitasScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Log Aktifitas Screen'));
  }
}
